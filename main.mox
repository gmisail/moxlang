import "vector.mox"

function main() -> int
    var vec : Vector = new Vector()

    vec.push(0)
    vec.push(3)
    vec.push(6)

    for(i from 0 to vec.count())
        printf("%i is %i\n", i, vec.get(i))
    end

    delete vec
end
