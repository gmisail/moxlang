import "mox/math.mox"
import "mox/std.mox"
import "mox/array.mox"

function main() -> int
    var arr : Array = new Array(10)
    var sum : int = 0

    for(i from 0 to 10)
        arr.set(i, i)
    end

    for(i from 0 to 10)
        sum = sum + arr.get(i)
    end

    printf("the sum is %i\n", sum)

    delete arr

    print("hello from the mox standard library!\n")

    printf("the distance between (1, 4) and (5, 9) is %f", dist(1, 4, 5, 9))

    return 0
end