import "examples/vector.mox"
import "examples/coordinate.mox"

function main() -> int
    var vec : Vector = new Vector()!
    var coord : Coordinate = new Coordinate(10, 5)!

    vec.push(0)
    vec.push(3)
    vec.push(6)

    for(i from 0 to vec.count())
        printf("%i is %i\n", i, vec.get(i))
    end

    printf("(%i, %i)\n", coord.x, coord.y)
end
