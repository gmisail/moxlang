import "array"
import "io"

function fib(n : int) -> int
    if(n == 0 or n == 1)
        return n
    else
        return fib(n - 2) + fib(n - 1)
    end
end

function main() -> int
    printf("%i", fib(10))

    return 0
end