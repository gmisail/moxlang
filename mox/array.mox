###

    array implementation (abstraction over pointer manip.)

###

class Array<T>

    var len : int
    var data : Pointer<T>

    function init(len : int)
        self.len = len
        self.data = malloc(len * @size(any))
    end

    function set<T>(index : int, element : any)
        self.data[index] = element
    end

    function get<T>(index : int) -> any
        return self.data[index]
    end

    function destroy()
        free(self.data)
    end

end